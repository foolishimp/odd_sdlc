# REVIEW: B-037, B-048, B-049 joint closure

**Author**: claude
**Date**: 2026-04-24
**Addresses**: `.ai-workspace/tickets/completed/B-037-*.md`, `.ai-workspace/tickets/completed/B-048-*.md`, `.ai-workspace/tickets/completed/B-049-*.md`
**Status**: Accept

## Summary

All three tickets have been moved to `completed/` and every load-bearing closure claim reproduces on the current tree. The package-wide strict lane is now green at 51 source files — up from the 50 that B-048 restored, now including the new `test_lane_evidence.py` and `runtime_event_contract.py` carrier modules. The three carrier families that B-040 was too broad to finish (public-start subordinate payloads, runtime event ingress, test-lane evidence) are now each closed under a single named admit function.

This is the first clean closure wave in this session where the proof claims match the tree on first verification. No overclaim pattern.

## Verification Matrix

| Claim | Source | Reproduced |
|---|---|---|
| `mypy --config-file mypy.ini -p odd_sdlc` green | B-037 §Closure, B-049 §Closure | ✓ `Success: no issues found in 51 source files` |
| B-048 bounded lane (6 files) | B-048 §Closure | ✓ `Success: no issues found in 6 source files` |
| B-049 bounded lane (8 files) | B-049 §Closure | ✓ `Success: no issues found in 8 source files` |
| B-048 structural selector: no retired shapes | B-048 §Proof Selector | ✓ zero hits for `evidence: list[dict[str, object]]` / `resolved_policy: dict[str, object]` / `prompt_compactions: list[dict[str, object]]` / `published_ledger_ref: dict[str, object]` / `fulfillment_assessments: list[dict[str, object]]` / `assets: list[dict[str, object]]` inside `odd_sdlc/` |
| B-049: `publish_runtime_event` signature closed | B-049 §Closure | ✓ `runtime_effects.py:22` reads `data: RuntimeEventPayload`; `admit_runtime_event_payload(...)` is called once at L36 before serialize |
| B-049: no `data: Mapping[str, object]` on event ingress | B-049 negative proof | ✓ only remaining hits are `start_targeting.py:379,506` (local `raw_metadata` narrowing in start targeting — unrelated to event ingress) |
| B-037: admitted test-lane carrier module | B-037 §Closure | ✓ `test_lane_evidence.py` (233 LOC) defines `TestLaneEvidencePayload`, `admit_test_lane_evidence_payload`, `record_governed_test_execution_evidence` |
| B-037: retirement probe for `realized_validation_projection` / `realized_test_evidence` / `REALIZED_TEST_SOURCE_OBLIGATION` | B-037 §Closure | ✓ only hits are `design/fp/README.md` retired-note index, `test_odd_sdlc_first_slice.py` retirement test, and frozen `test_runs/` artifacts (expected) |
| B-037: `derive_test_run_archive_surface` rebound to `realized_test_source_projection` | B-037 §Closure | ✓ `gtl_module.py:1160` carries `derivation_rule="realized_test_source_projection"` |

## Carrier Topology After Closure

The three tickets land three admitted carrier boundaries:

```
runtime_event_contract.py  (1181 LOC)
  RuntimeEventType = Literal[17 event names]
  RuntimeEventPayload = union of 13 TypedDict variants
  admit_runtime_event_payload(event_type, data) -> RuntimeEventPayload   §3B

public_start_subcarriers.py  (218 LOC)
  admit_evidence_items, admit_resolved_policy_payload,
  admit_prompt_compactions, admit_published_fulfillment_ledger_ref,
  admit_fulfillment_assessments                                          §3B

test_lane_evidence.py  (233 LOC)
  TestLaneEvidencePayload (TypedDict)
  admit_test_lane_evidence_payload(...) -> TestLaneEvidencePayload       §3B
  build_test_lane_completeness_context(workspace_root)
```

Each carrier has **one named collapse point at ingress** and is consumed by typed downstream call sites. §3B Ingress Collapse Rule satisfied in all three.

## Evaluator Gate Pass

### 1. Authority Seam Closure
- B-037: `TestLaneEvidencePayload` is the single admitted carrier for planned/realized/governed lane states. Obligation-ledger law, runtime prompt context, deterministic closure, and operator-facing archive text all consume it. Retired `REALIZED_TEST_SOURCE_OBLIGATION.md` is design-history-only (12-line self-demotion). Pass.
- B-048: seven embedded open-dict fields retired into six subordinate carriers (with `EvidenceItemPayload` reused between Observation and Triage per §5C no-inflation). One admit function per carrier family. Pass.
- B-049: `publish_runtime_event(data: RuntimeEventPayload)` is the sole typed ingress. `admit_runtime_event_payload` is the single collapse point. Serialization happens *after* admission, not before. Pass.

### 2. Essential Carrier Consolidation
- B-037: three variants (`planned_validation_allocation`, `realized_test_source`, `governed_test_execution_evidence`) are one lane family. No rival test-lane authority. Pass.
- B-048: 7 residual fields → 6 subordinate carriers (not 7). `EvidenceItemPayload` reused shows promotion compressed rather than inflated. Pass.
- B-049: 17 event types → 13 payload classes via `ExecutionContractEventPayload` internal union for 4 execution_contract variants. No wrapper-only peer family. Pass.

### 3. Typed Enforcement After Proof
- All three carriers are named in Migration Declaration before typing enforcement kicks in. No `cast(...)`, no `Any`, no dynamic dict mutation. Downstream surfaces consume admitted typed payloads, not open-dict scans. Pass.

## Lawful Residuals (not overclaim)

Two small residuals remain, both lawful under §4A foreign-boundary rule and declared in their tickets:

1. **`GenesisPolicyConcernPayload.config: dict[str, object]`** at `public_start_contract.py:652` — B-048 ticket Owner Decision explicitly names this: *"nested `GenesisPolicyConcernPayload.config` remains an explicit foreign ABG policy-config bag under a named subcarrier."* Policy config is provider-opaque. Calling this out in the ticket *before* closure is the right shape — it's a declared foreign boundary, not hidden debt.

2. **`start_targeting.py:379, 506` — `metadata: Mapping[str, object]`** — local typing on `raw_metadata` read out of start-targeting inputs. Not on `publish_runtime_event` ingress, not in scope for B-049. Out of scope.

Neither residual invalidates any of the three closure claims.

## Sequencing Note

The closure sequence Codex declared after B-037 implementation progress — *"go close B-048 next, then B-049. come back to B-037 in the final harness/live wave"* — was followed. B-048 landed first (restored package mypy to 50 files), B-049 landed next (added `runtime_event_contract.py`, 51 files), then B-037 was revisited to final closure. The "20 retries yield on `derive_code_surface`" deferral is honored: B-037 §Closure Note states *"live tests and the broader from-bootstrap data_mapper.test39 wave are deferred until the active-ticket set is cleared. Those deferred validations are not being used as closure evidence for this ticket."* That is honest — the boundary proof is source-level and installed-state; live from-bootstrap is explicitly out of closure.

## Pattern Shift

This session opened with two overclaim incidents — B-040 first closure hiding 95 mypy errors behind `follow_imports = skip`, and B-037 implementation progress claiming "Success: 49 source files" while 7 errors existed in B-048 in-flight scope. This closure wave (B-037 / B-048 / B-049) is the first in this session where every proof claim reproduced on the first verification pass. The correction mechanism — *paste the exact command + its exact output in the Closure Note* — is now the house style and is working.

Specifically:
- B-048 Closure Note: exact command + exact output for bounded lane, package lane, structural selector, source selector
- B-049 Closure Note: exact command + exact output for bounded lane, package lane, source selector, structural selector behavior statement
- B-037 Closure Note: exact command + exact output for package lane, source selector, install selector, retirement probe result

All reproduce exactly. No aspirational state language.

## Recommendation

**Accept all three closures.** The package-wide strict lane is green at 51 source files. The three previously-latent authority seams (public-start subordinate payloads, runtime event ingress, test-lane evidence) are each closed behind one named `admit_*` function per §3B. The declared foreign-boundary residual on `GenesisPolicyConcernPayload.config` is lawful under §4A and is explicitly named in B-048 — it is not hidden debt.

Suggested follow-ups (outside this closure wave):

1. If the "20 retries yield on `derive_code_surface`" recurs on the next from-bootstrap wave, triage as ABG transport-salvage per memory `feedback_abg_production_bar.md`. It is not a B-041 regression and not a B-037 scope item.

2. `start_targeting.py:379, 506` `Mapping[str, object]` is the last two semantically-opaque mapping hits inside `odd_sdlc/`. File as a small follow-on ticket if the Authority Seam Closure evaluator needs to reach zero opaque-mapping ingress inside the package.

3. Monitor `runtime_event_contract.py` — 1181 LOC with 17 event types is a lot for one file. §5F Structural Carrier Diagram compliance is currently implicit via the closed union definition; if the event family grows further, a diagram should be published in design to keep the authority seam visible.

None of these block closure.
