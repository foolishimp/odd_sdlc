# T-204 Cross-Repo Review Verdict — build validation + code review (odd_sdlc ⇄ abiogenesis)

State reviewed: working tree at odd_sdlc HEAD `d7dac6b` + all uncommitted changes; abiogenesis HEAD `f169fb6` (T-159 closed) consumed as release `4.1.0-rc.2`. Two adversarially-verified workflows merged (27 + 42 agents; 35 confirmed, 14 refuted), deduped, with the two HIGHs hand-verified. Commentary, not ratified.

## 1. Build validation — both repos green
| Check | Result |
|---|---|
| odd_sdlc `build:semantic` (tsc + GTL preflight) | ✅ |
| odd_sdlc `lint:semantic` | ✅ 0 warnings |
| odd_sdlc focused proof lane (t140, t203, t197, t161, t184, pack guard) | ✅ 111/111 |
| ABG `build:semantic` | ✅ exit 0 |
| ABG `4.1.0-rc.2` (T-159 boundary) pinned + installed | ✅ |

Build-green validates compile/lint/typed-carrier retirement. It does **not** validate the behavior/boundary findings below.

## 2. Headline
- **The documented data-mapper regression is FIXED** in the working tree (`review_grade_edge_fulfillment.ts:1543-1550` reorders scope precedence so `inlineObligationIds` wins before the `full_breadth` fallback; prompt matches; `test_t182:1981` guards it).
- **That fix is owned by odd_sdlc, not abiogenesis.** ABG `4.1.0-rc.2` already ships `same_edge_retry → repair_same_edge` / `retry_same_edge; retryEligible=true` (verified by extracting the consumed `.tgz`). The ticket's Closure Implications imply a missing ABG continuation — **the code refutes that; no new ABG capability is required.**
- **But this diff introduces TWO new HIGH defects beyond the known one** — both re-grow "archive-scrape-as-authority" decision points that T-204 bans, both net-new, both latent (code-reachable, not yet observed firing), neither test-covered.

## 3. Findings by severity (merged, deduped)

### HIGH

**H1 · `currentPostflight` review-grade short-circuit downgrades repairable pressure to non-retryable `triage_gap` · odd_sdlc · `operator/installed_operator.ts:4222-4280`**
Net-new gate (at HEAD this function had no `currentPostflight` param): if `activePostflightBlockingReasonCarriers(input.currentPostflight)` is non-empty (`:336-338`, **no reentry-class filter**), it re-codes **every** carrier to `review_grade_assessment_invalid` → `lawfulReentryPoint:"triage_gap"` (`shared/blocking_reason.ts:415-422`) and **overwrites the existing gap dossier** (`:4248-4254`). `component_code_surface` isn't review-grade-exempt (`edge_output_policy.ts:45-60`), so repairable codes (`materialized_product_files_missing`, `obligation_unassessed`, `obligation_fulfilled_without_output_coverage` → `same_edge_retry`; `obligation_blocked_without_evidence` → `repair_worker_output`) are converted to non-retryable, and the dossier the retry gate reads (`installed_operator.ts:1019,1202`) suppresses the same-edge retry. Reachable on the deferred-consequence success path (`:9444/:9448`). It re-opens the exact downgrade the admission-scope fix just closed, on a different trigger, over a broader carrier class. **Violates** ticket Closure Implication #2 (current-edge semantic repair must stay repairable). **Fix:** filter `currentPostflightBlockers` by reason class — pass through `same_edge_retry`/`repair_worker_output`/`escalate_to_fp` carriers, don't relabel them, don't overwrite a dossier naming a stronger re-entry; add a component-code repairable-blocked test. **Caveat:** latent — not observed firing in the cited archive (its compute postflight passed).

**H2 · Predecessor-design selection scrapes ABG closure disposition as acceptance authority + over-accepts non-converged predecessors · odd_sdlc · `operator/plugins/evaluate/design_depth_register.ts:352-393` (consumed `:401-418`)**
Net-new `predecessorDesignRegisterArchiveIsAccepted` reads `sdlc_edge_closure_decision.json` and treats `disposition==="close"` (`:362`), else falls through to `postflight.json status==="passed" && fp_evaluate_result.json status!=="blocked"` (`:389`) as acceptance. **Two defects:** (a) **boundary** — it scrapes ABG-authored closure truth as authority in the live admission path; the T-204 audit marks `edgeClosureDispositionFromArchive`/`…DecisionFromArchive` `move_to_abg` and `register_purpose.ts:182-191` in the *same diff* declares this carrier `authorityUse: abg_admitted_runtime_truth` — self-contradiction. (b) **correctness** — on `reprice`/`retry`/`yield`/`deferred` it falls through and returns `true` when postflight passed, so an explicitly-not-converged predecessor is "accepted"; and because the consumer selects `acceptedArchiveRoots` **exclusively when non-empty** (`:412-413`), when no predecessor genuinely closed this puts the *repriced* one alone in the selected set and **drops the other candidate registers' evidence** (I verified this consumer logic). **Fix:** consume an ABG-admitted accepted-predecessor set, or restrict to the existing structural `predecessorDesignRegisterArchiveMatchesCurrent` without reading disposition/status; at minimum return `false` on non-`close` dispositions. Don't invert t181 — the behavior is the bug. *(Run 1 rated this Low "only re-prioritizes"; run 2 + my trace show it can drop evidence — High.)*

### MEDIUM

**M1 · odd_sdlc still authors ABG-owned ledger/closure/next-action archives · `installed_operator.ts:7446` (`deriveInstalledTraversalConsequence`), `:8110-8173` (`writeTraversalConsequenceArchive`)** — the shared `SdlcInstalledOperatorTraversalConsequence` type was retired (good), but `traversal_consequence.ts` has zero diff and odd_sdlc still locally folds + writes `sdlc_edge_fulfillment_ledger.json` / `sdlc_edge_closure_decision.json` / `sdlc_next_action_projection.json`. T-159 (line 236) says plugins cannot write ledgers/transition/replay. Honestly disclosed as next-cut. **Fix:** move authorship to ABG ledger interfaces; keep only SDLC-domain fold meaning.

**M2 · Worker retry-context derived by scraping archive JSON; required guard absent (two paths) · `installed_operator.ts` `deriveSdlcWorkerRetryContextFromPostActionProjection:910` (live `:8540`) + `mergeSdlcWorkerRetryContextWithRuntimeGapRegister:1190` → `latestRuntimeGapDossierForRetryContext:998` (live `:8547`)** — after the diff deleted the consequence-based deriver, these archive-scraping derivers (`readFileSync`/`readdirSync` of `gap_dossier.json`/`sdlc_edge_closure_decision.json`/`handoff_manifest.json`) still feed the worker handoff `retryContext`. The T-204-demanded guard rejecting archive-JSON-as-authority is **unimplemented**; t197 only denylists two named symbols. **Fix:** consume ABG-admitted projection facts; add a structural gate failing on `readFileSync(... .json)` of those archive files as authority, covering both derivers.

**M3 · Surviving public gaps API authors gap/closure/bind-outcome from archive scraping · `workspace_api/entry.ts:893` (`projectOddSdlcWorkspaceGaps`) → `:735` → `:527` (`edgeFulfillmentLedgerFromArchive:392`, `edgeClosureDecisionFromArchive:429`, `nextActionProjectionFromArchive:449`); `missingBindOutcomeAfterPassedComputeRefs:541` decides a gap purely from file presence** — pre-existing (not modified this tree), but it locally authors bind-outcome/closure judgments from raw archive JSON. Note: the survival inventory (corrected 2026-06-21) blesses this file `product_projection / survive` — correct for the *file's* home, but it does not surface the **embedded `move_to_abg` scraping** inside it. **Fix:** reprice onto ABG-admitted projections (move bind-outcome-after-passed-compute to ABG); annotate the inventory row; don't close T-204 while this survivor authors bind-outcome gaps from file presence.

**M4 · odd_sdlc installer authors a runtime event into ABG's canonical `events.jsonl`; guards scoped to the wrong file · `operator/event_store.ts:58` (`appendOddSdlcRuntimeEvents`, write `:71`), `install/installer.ts:37` (sole author of `workspace_installation_admitted`, called `:393`)** — ABG's `emit()` only normalizes; the `appendFile` persistence is odd_sdlc-owned, writing into the same canonical runtime log ABG reads. This exceeds the "non-authoritative read/test-compat only" license. The authorship guards (`test_t151:947`, `test_t064:924`) only assert `installed_operator.ts` omits the call — neither covers `event_store.ts`/`installer.ts`, giving false-green. **Fix:** move install-lifecycle authorship behind an ABG-admitted event API, or explicitly classify odd_sdlc install authorship and widen the guards.

**M5 · Ownership correction (informational) · cross-repo** — the documented regression is an odd_sdlc adapter/classification fix, NOT a missing ABG capability (see Headline). The ticket's Closure Implications #2/#4 should be repriced to reflect that ABG `4.1.0-rc.2` already exposes the same-edge repair continuation.

### LOW
- **Confirmatory:** documented admission-scope regression FIXED (`review_grade_edge_fulfillment.ts:1543-1550`); consequence-bind *live* path is T-159 conformant (odd_sdlc proposes via `constructSdlcConsequenceTraversalActionBinding` → ABG `admitConsequenceTraversalActionForAllowedCatalog`, never executes locally); deleted retry/terminal helpers + effects/closed-F_D types were already dead, surviving paths intact; ABG uncommitted lever work is provably absent from the consumed `4.1.0-rc.2` `.tgz` (no boundary confound).
- `config/operator-runtime-policy.json:16-19` — review-grade evaluator timeout cut 12h→15min, inactivity 30min→7min, floor removed; `review_grade_evaluator_process_timeout` maps to non-retryable `triage_gap`. Sanity-check 15min suffices under live data-mapper load (observed ~2-4.5min), or classify it `same_edge_retry`.
- `closure_state_machine.ts:986` (re-exported `operator/index.ts:468`) `syntheticGapDossiersFromClosureDecision` orphaned after its only caller was deleted — delete it.
- `product_materialization/authority.ts:2057-2076` `mergeAuthorityTargets` file→directory coercion widens admission (whole subtree), unguarded — add a blessing test (intra-odd_sdlc, not a boundary violation).
- `plugins/evaluate/content_register.ts:41,43` `_LEDGER_ROW_KIND`/`_LEDGER_VERSION` dead exports (`_LEDGER_KIND` is live) — wire or drop.
- Hygiene: `walkFiles` now prunes `.ai-workspace/runtime/*` for all targets (benign); design-depth candidate-evidence containment guard drops evidence refs for predecessor-sourced admissions (trail completeness only).
- **abiogenesis:** `m03/contracts/event_admission.ts:509-527` `lever_resolution_admitted` admits internally-inconsistent runner-retry payloads (sole producer is consistent; add all-or-none + negative test); `shared/lever_registry/registry.ts:104-111` `consumedAt` cites wrong line (`app/m04/start.ts:58` vs real `:68`/`:130`).
- **Working-tree hygiene (pre-commit):** two untracked junk files in `build_tenants/typescript/` — `"[]\""` (162-byte shell-redirect artifact) and `isolate-0x86fc0c000-34474-v8.log` (V8 profiler log). Both would be swept into a `git add -A`. Delete + optionally `.gitignore` them. (I did not delete them — they're not mine; flagging for your call.)

## 4. Cross-repo boundary
The consequence-bind **live action** path is T-159 conformant (propose → ABG admit → hand back; nothing executed locally), and the T-159 bind-boundary law in `gtl_program_conformance.ts` is shipped in rc.2 and unchanged. **However**, the net boundary posture of this diff is mixed: it retired the shared consequence carrier *type* but did **not** move the runtime authorship (M1), left two unguarded archive-scrape retry paths (M2) and the public gaps scraper (M3), introduced a new install-event authorship write (M4), and **grew two new archive-scrape decision points** (H1 `currentPostflight`, H2 `predecessorDesignRegisterArchiveIsAccepted`). The "no archive-JSON-as-authority" guard the ticket demands is still absent, which is why these regrew unnoticed. T-204 Closure Implications #1/#3/#5 remain open.

## 5. Bottom line
**Checkpoint OK to land (build green; documented break fixed; consequence-bind live path conformant) — but do NOT mark T-204 closeable, and track/triage the two new HIGHs before relying on same-edge repair for component-code or on predecessor-design selection.** Single most important item: **H1 `installed_operator.ts:4222-4280`** — the new `currentPostflight` short-circuit re-opens the non-retryable `triage_gap` downgrade for repairable blockers; **H2 `design_depth_register.ts:352-393`** is the boundary-plus-bug twin. Both are odd_sdlc-owned, net-new in this diff, latent, and untested. The one corrective conclusion for the ticket: the same-edge repair continuation is an **odd_sdlc** classification fix, not a missing ABG capability — ABG `4.1.0-rc.2` already ships it. Add the demanded archive-scrape guard to stop this defect class from regrowing.
