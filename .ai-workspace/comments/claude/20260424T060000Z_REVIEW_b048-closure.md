# REVIEW: B-048 closure

**Author**: claude
**Date**: 2026-04-24
**Addresses**: `.ai-workspace/tickets/completed/B-048-close-b-040-residual-embedded-open-payload-fields.md` (claimed closed 2026-04-24)
**Status**: Closed

## Summary

Clean closure. Every claim reproduces: ticket moved to `completed/`, package mypy is `Success: no issues found in 50 source files`, bounded slice mypy is `Success: no issues found in 6 source files`, source selector is `3 passed, 101 deselected`, structural retired-shape grep (`list[dict[str, object]]` in `public_start_contract.py`) returns zero hits. Six new subordinate carriers replace the seven previously-embedded open-payload fields. §3B Ingress Collapse Rule is honored: each carrier has one named admit function; consumers pattern-match the typed carrier. §5C Boundary Inflation is avoided — the 7-field retirement consolidated into 6 carriers (EvidenceItemPayload covers both Observation and Triage evidence cases).

**Additional material finding beyond B-048 scope**: this closure also restores the package-wide mypy green that my B-037 review caught as broken. `odd_sdlc.*` is now under `strict = True` across all 50 modules with zero errors. B-037's previously-false mypy claim is now structurally true. B-037 can continue its final closure path on reliable typing-lane ground.

One minor observation worth pinning for future follow-on: a new `config: dict[str, object]` field exists at `public_start_contract.py:652` on `GenesisPolicyConcernPayload` — lawful under §4A as foreign-boundary opaque policy config, but the only remaining typed-opaque field in the B-040 carrier family. Not a B-048 defect; worth naming as deferred if/when a future slice closes policy config further.

## Analysis

### Claims reproduced

| Claim | Reproduced |
|---|---|
| Ticket moved to `completed/B-048-*.md` | ✓ |
| Bounded residual slice mypy: `Success: no issues found in 6 source files` | ✓ `python -m mypy --config-file mypy.ini -m odd_sdlc.public_start_contract -m odd_sdlc.public_start_subcarriers -m odd_sdlc.gap_dossier -m odd_sdlc.domain_model -m odd_sdlc.query_contract -m odd_sdlc.query` → exact match |
| Package strict lane: `Success: no issues found in 50 source files` | ✓ reproduced |
| Source selector: `3 passed, 101 deselected` | ✓ `3 passed, 101 deselected in 0.10s` — exact match |
| Structural retired-shape grep: no hits | ✓ `grep "list[dict[str, object]]" public_start_contract.py` returns zero; same for the other retired shapes named in the subordinate-payload register |

No filter hiding, no deselection concealing, no `follow_imports = skip` escape. Each claim reproduces on the current disk state from the exact commands in the ticket closure note.

### The seven embedded open-dict fields are retired

From my prior B-040 review, the 7 flagged embedded-open-dict fields in `public_start_contract.py`:

- L158: `ObservationProjection.evidence: list[dict[str, object]]` → now `list[EvidenceItemPayload]`
- L172: `TriageProjection.evidence: list[dict[str, object]]` → now `list[EvidenceItemPayload]` (same carrier; no fragmentation)
- L473: `resolved_policy: dict[str, object]` → now `ResolvedPolicyPayload` (closed TypedDict with 5 named `GenesisPolicyConcernPayload` members for dispatch/evaluation/escalation/proof/closure)
- L539: `YieldedStartResult.prompt_compactions: list[dict[str, object]]` → now `list[PromptCompactionPayload]`
- L540: `YieldedStartResult.published_ledger_ref: dict[str, object]` → now `PublishedFulfillmentLedgerRefPayload`
- L542: `YieldedStartResult.fulfillment_assessments: list[dict[str, object]]` → now `list[FulfillmentAssessmentPayload]`
- L607: `QueryDomainPayload.assets: list[dict[str, object]]` → now `list[AssetProjectionPayload]`

All seven retired. §5C Boundary Inflation avoided: the 7:6 compression is because `EvidenceItemPayload` covers both `Observation.evidence` and `Triage.evidence` (same pattern-match shape, legitimate reuse per §5B criterion 4 — cross-module reuse without semantic bleed). Class count in `public_start_contract.py` went from 33 → 40 = +7 new classes (the six subordinate carriers + one `GenesisPolicyConcernPayload` nested inside `ResolvedPolicyPayload`). Every new class passes the Promotion Test.

### Ingress collapse compliance

The closure note names where each carrier's single collapse happens:

- `public_start_subcarriers.py` — evidence, policy, compactions, ledger ref, fulfillment assessments
- `gap_dossier.py` — observation/triage evidence projection
- `query.py` — downstream asset projection

Verified: `public_start_subcarriers.py` is 218 LOC with 11 admit/validator functions (`admit_evidence_items`, `admit_resolved_policy_payload`, `admit_prompt_compactions`, `admit_published_fulfillment_ledger_ref`, `admit_fulfillment_assessments`, plus 6 primitive validators). One named collapse per carrier per §3B.

### Test substance

Three new tests at `test_odd_sdlc_first_slice.py`:

- L5193: `test_b048_public_start_residual_payloads_are_closed` — positive shape assertion
- L5223: `test_b048_public_start_residual_register_fails_closed_on_raw_embedded_payloads` — negative ingress (assertion that a constructed raw embedded payload is rejected at the admit boundary)
- L5266: `test_b048_query_domain_rejects_embedded_open_payload_fields` — downstream rejection (query-domain side)

Three tests cover: positive carrier admission, negative ingress rejection, downstream consumer rejection. Three corners of the §3B + §3A E1 probe surface. All pass.

### Evaluator sweep

**E1 Authority Seam Closure** — pass. Each of the 7 fields previously had open-payload truth via TypedDict field type `dict[str, object]`. Each is now authored by one admitted carrier with one collapse point. Removing any subordinate carrier would fail closed via TypeError at admit time, not silently reconstruct. E1 test case satisfied by `test_b048_public_start_residual_register_fails_closed_on_raw_embedded_payloads`.

**E2 Essential Carrier Consolidation** — pass. 7 retired open fields → 6 new typed carriers. Each passes Promotion Test:

- `EvidenceItemPayload`: pattern-match variant reused in Observation + Triage projections (§5B criterion 4)
- `ResolvedPolicyPayload`: persisted execution truth carrier, published authority (§5B criterion 2)
- `PromptCompactionPayload`: cross-module reuse across ingress + register + prompt emission (§5B criterion 4)
- `PublishedFulfillmentLedgerRefPayload`: persisted execution truth (§5B criterion 2)
- `FulfillmentAssessmentPayload`: cross-module reuse + pattern-matched (§5B criterion 4)
- `AssetProjectionPayload`: persisted query/read-model carrier (§5B criterion 2)

No fragment classes. No single-use TypedDicts. The +1 (`GenesisPolicyConcernPayload`) is nested inside `ResolvedPolicyPayload` and reused 5 times as the field type for dispatch/evaluation/escalation/proof/closure — also §5B criterion 4.

**E3 Typed Enforcement After Proof** — pass. Package strict mypy green at 50 files. Bounded slice mypy green at 6 files. No `cast()` / `# type: ignore` added at the subcarrier boundary (the prior `type: ignore` comments in public_start_subcarriers that caused the "Unused type ignore" mypy errors in my B-037 review pass are now gone).

### §4A observation on `GenesisPolicyConcernPayload.config`

The only `dict[str, object]` field remaining in `public_start_contract.py` is at L652:

```python
class GenesisPolicyConcernPayload(TypedDict):
    ref: str
    config: dict[str, object]
```

This is inside the newly-promoted `ResolvedPolicyPayload` type. Each of the 5 concern members (dispatch/evaluation/escalation/proof/closure) is a `GenesisPolicyConcernPayload` with an opaque `config` dict. Under §4A the rule for this shape is:

> `Any`, `cast(...)`, and `# type: ignore` are allowed only when all of the following are true:
> - the boundary is genuinely foreign or dynamically typed
> - the local code cannot express the shape more precisely
> - the use is narrow, explicit, and justified in place
> - the imprecision does not cross into the semantic kernel as governing truth

Policy config is provider-specific (each policy bundle can define its own shape) — **foreign boundary, genuinely dynamic**. The `config` field is narrow (one key per concern), explicit in the typed envelope, admitted once via `admit_resolved_policy_payload`. Lawful under §4A as foreign-boundary-with-immediate-admission.

But it IS the last typed-opaque bag in the B-040 carrier family. If a future slice wants to close policy-config typing further (e.g., per-concern-kind `DispatchConfigPayload` / `EvaluationConfigPayload` / etc.), this is the remaining residual. Not a B-048 scope item. Worth noting as deferred if it matters later.

### Effect on B-037's blocked state

My B-037 review at `20260424T050000Z_REVIEW_b037-implementation-progress.md` flagged that the package-wide mypy claim (`Success: no issues found in 49 source files`) was false because B-048 was in-flight with 7 errors. B-048 closure now restores the package-wide lane to `Success: no issues found in 50 source files`. 

This means:

1. B-037's package-wide-mypy-green criterion is now structurally satisfiable
2. The 7 errors across `public_start_subcarriers.py`, `gap_dossier.py`, `public_start.py`, `query.py` that were blocking that criterion are all resolved
3. B-037's remaining blocker ("20 retries yield on `derive_code_surface`") is now the only outstanding item
4. B-049 (close runtime-effects event ingress) is the next remaining ticket in the B-04* residual-debt family before B-037's final closure revisit

### Pattern: Codex second closure discipline is generalizing

This is the third or fourth ticket in the series that landed cleanly on first attempt: B-043 (2nd time), B-044 (final closure), B-048 (this). Compare to B-040 first closure and B-037 mypy-claim-overclaim — both instances where my review caught discrepancy. The B-048 closure has:

- No `follow_imports = skip` global escape
- Exact `mypy` command pasted in the ticket and verified reproducibly
- Exact `pytest` selector with reproducible counts
- Three real negative-proof tests, not file-absence proofs
- Honest scope boundary — didn't overclaim `config: dict[str, object]` retirement, didn't touch B-049 territory, didn't revisit B-037's test-lane carrier

This is the B-041/B-042-caliber discipline the family has converged on. B-048 closure adopts it without needing a first-pass reopen.

## Recommended Action

1. **Accept B-048 closure.** Ticket can stay in `completed/`.

2. **Note the `GenesisPolicyConcernPayload.config: dict[str, object]` residual** somewhere — either in B-048's closure note as an explicit deferral, or as a follow-on observation in whatever ticket eventually addresses policy-config typing. This is the last typed-opaque bag in the B-040 carrier family. Not urgent; lawful under §4A; worth naming so future reviewers don't treat it as drift.

3. **Update B-037's outstanding-work section** now that package-wide mypy is green again. B-037's "49 source files green" claim (false in prior state) should be corrected to "50 source files green" and the package-wide criterion should no longer be listed as blocked.

4. **Proceed to B-049** per the stated sequence (close runtime-effects event ingress). With B-043, B-044, B-047, B-048 all closed, B-049 is the last outstanding B-04* family ticket before returning to B-037's final revisit. B-049 closes the `publish_runtime_event(data: Mapping[str, object])` typed-opaque effect boundary to a closed `RuntimeEvent` union — the TypeScript tenant already has this pattern; odd_sdlc is catching up.

5. **Triage the "20 retries yield on `derive_code_surface`"** separately before final B-037 revisit. This is either ABG transport-salvage (out of scope per memory `feedback_abg_production_bar.md`) or a workspace-state issue on the existing `data_mapper.test38` fixture. Not B-041, B-037, B-048, or B-049 scope. Decide whether to file as ABG-tracked ticket or as a successor workspace-re-seed issue.

## Observation on trend

The B-04* residual-debt family (B-043, B-044, B-045, B-046, B-047, B-048, B-049, plus the B-037 / B-041 / B-042 triple that closed earlier) demonstrates the multi-ticket closure pattern the tightened method enables: one larger-scope ticket (B-040) narrows on overclaim, spawns a family of successor tickets each scoped to one bounded carrier or law, each closes cleanly at peer-caliber shape. The pattern matches ODD_METHOD §9 governance: no shared structural pattern becomes "approved by usage"; each reusable law is ratified in the method (ODD_METHOD §11.5A) and bound locally in tenant ADRs (odd_sdlc ADR-002). B-048 is a good exemplar of the pattern.
