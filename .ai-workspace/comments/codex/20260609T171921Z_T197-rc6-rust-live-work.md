# T-197 / ABI 4.0.0-rc.6 Rust Live Work

Author: codex
Date: 2026-06-09T17:19:21Z
Status: posted

## Summary

Closed the requested Rust hello-service live proof on the current T-197 / ABI 4.0.0-rc.6 slice.

The clean live archive is:

`build_tenants/typescript/test_env/test_runs/scenario_t164_rust_hello_service_lite_live/20260609T164901885Z_pid35003`

Command:

```sh
npm run test:t164:rust-service-live
```

Result:

```text
pass 1, fail 0
duration_ms 1775100.532667
```

## Live Proof

Observed edge sequence:

```text
derive_intent_surface
derive_lite_design_adr_surface
derive_lite_component_code_surface
prepare_test_execution_surface
derive_test_execution_result_surface
```

All five edge archives closed with `sdlc_edge_closure_decision.disposition: close`.

Final execution evidence:

```json
{
  "kind": "sdlc_worker_execution_evidence",
  "status": "succeeded",
  "testsObserved": 1,
  "passedCount": 1,
  "failedCount": 0
}
```

The execution shard stdout was:

```text
helloworld
```

## Work Completed

- ABIogenesis TypeScript tenant was cut and pushed as `4.0.0-rc.6`.
- SDLC now consumes ABI rc.6 from `release_snapshots/abiogenesis-typescript-tenant/latest`.
- The rc.6 route fixed the downstream graph-span use case: `applyGraphSpanReentryRoute(...)` accepts open product assessment spans instead of requiring endpoint-closed vectors.
- T-197 W-110 slice D consumes:
  - `applyExplicitGraphVectorResumeCursor(...)`
  - `applyGraphSpanReentryRoute(...)`
- Production `construct*Event(...)` sites under `code/src` are removed; only B-068 proof-fixture constructors remain in the explicit test allowlist.
- Deterministic verification before live:
  - `npm run build:semantic`
  - `npm run test:t197` 15/15
  - `npm run test:t184` 21/21
  - `npm run test:t164` 22/22 plus 1/1 Rust sandbox
  - `npm run lint:semantic`
  - `npm run lint:test-harness`
  - `node --test test_env/tests/test_t191_typed_prompt_assets.test.mjs` 10/10
  - `npm run test:semantic` 959/959

## Issues Found

- First live attempt archive `20260609T163259707Z_pid14479` blocked on `design_depth_fp_evaluator_process_failed` after a provider socket close. This was not normalized into retry because T-188 / requirement law says evaluator process failures block rather than become same-edge product retries.
- The failed live attempt then surfaced as a final missing `Cargo.toml` scenario assertion because the graph stopped before the code/test edges. The successful rerun proves this was caused by the evaluator transport block, not by the lite overlay sequence.
- One stale rc.5 regex in `test_t191_typed_prompt_assets.test.mjs` was updated before the full semantic suite passed.

## Remaining T-197 Work

T-197 stays active. Current known open work:

- A5: first-class ABG transition/ref consumption in SDLC consequence projection.
- A2: installed `--until converged` ownership disposition after A5 is settled.
- B2/B3: GTL read-model cleanup for component-depth register and prompt assets.
- Later hygiene rows: D/C/H/P-series items already tracked in the ticket.
