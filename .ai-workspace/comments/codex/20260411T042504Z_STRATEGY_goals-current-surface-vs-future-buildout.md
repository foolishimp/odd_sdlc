# STRATEGY: Goals Current Surface Vs Future Build-Out

**Author**: codex
**Date**: 2026-04-11T04:25:04Z
**Addresses**: `specification/GOALS.md`, `specification/INTENT.md`, `specification/PRODUCT.md`, `specification/requirements/03-runtime-governance.md`, `specification/requirements/10-odd-sdlc-software-domain-buildout.md`
**Status**: Draft

## Summary

`GOALS.md` in `odd_method` had drifted from bounded work-wave orientation into
a mixed surface containing future product build-out, requirement-like success
criteria, and proving-lane planning.

The current edit restores `GOALS.md` to present-tense wave focus and leaves the
future-oriented material here as commentary until it is intentionally repriced
into live constitutional surfaces.

## Analysis

The governing method says `GOALS.md` is the bounded work-wave surface and keeps
temporary focus out of deeper constitutional layers until it is intentionally
repriced there.

The current live invariant surfaces already carry the ratified descriptors that
should remain primary:

- `specification/INTENT.md` defines the directional line, including iterative
  closure, explicit runtime fact ownership, and the subordinate orchestration
  boundary.
- `specification/PRODUCT.md` defines the current product realization, including
  `odd_sdlc` as the first live software-domain package, the worksite lifecycle,
  runtime return, and the current proving subset.
- `specification/requirements/03-runtime-governance.md` defines the live
  runtime-boundary obligations.
- `specification/requirements/10-odd-sdlc-software-domain-buildout.md` defines
  the live software-domain obligations for ambiguity governance,
  ambiguity-aware convergence, iterative carry-forward, and traceability.

The material removed from `GOALS.md` falls into four future-leaning classes:

1. build-out direction that extends beyond current-wave orientation
2. requirement-like success criteria for ambiguity and closure behavior
3. named proving-lane targets such as `test23`
4. detailed strategy boundaries for major vs micro ambiguity handling

That material is not discarded. It remains useful as strategic commentary for
future repricing, but it should not sit in `GOALS.md` as if it were already the
current operative constitutional surface.

The specific future-facing themes preserved here are:

- further expansion of the software-domain package beyond the currently proven
  bounded subset
- future tightening of ambiguity handling, closure semantics, and policy
  reporting beyond what the active requirements already ratify
- future proving-lane planning for named test waves
- future repricing around broader package evolution while `odd_service`
  continues to incubate

## Recommended Action

- keep `GOALS.md` limited to present-tense wave orientation plus explicit trace
  boundaries back to live invariant surfaces
- reprice only ratified current reality into `INTENT.md`, `PRODUCT.md`, or
  `specification/requirements/` when the change is already live
- keep future build-out planning in strategy posts or tickets until one bounded
  wave is actively opened and the content becomes current
