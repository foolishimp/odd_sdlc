# Product

**Status**: Active
**Derived From**: `T-160 Rust lite fixture`

`t160_hello_world_rust_lite` is a single-tenant Rust hello-world product.

## Product Identity

- product name: `t160_hello_world_rust_lite`
- active tenant: `hello_world_rust`
- runtime: `cargo`
- selected output root: `build_tenants/hello_world_rust`

## Product Files

- `build_tenants/hello_world_rust/Cargo.toml` is generated by traversal and must
  not be present in the fixture.
- `build_tenants/hello_world_rust/src/main.rs` is generated by traversal and
  must not be present in the fixture.

## Exact Output Contract

```text
Hello, world!
```

## Declared Execution Command

```bash
cd build_tenants/hello_world_rust && cargo run --quiet
```

