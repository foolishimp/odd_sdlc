# Goals

**Status**: Active
**Derived From**: `T-174 live fixture`

- materialize the JavaScript tenant from staged implementation and test
  topology
- publish `sdlc_live_fp_parallel_materialization_frontier.json` from the
  installed `derive_component_code_surface` run
- show four ready lanes before fan-in: hello source, world source, hello test,
  and world test
- prove the generated tenant by importing the public module and running the
  Node.js branch tests
