# Intent

**Status**: Active
**Derived From**: `T-174 live fixture`

`t174_parallel_hello_world_js` exists to prove that `odd_sdlc` can publish an
admitted SDLC feature dependency DAG into an ABG-owned frontier before product
materialization closes.

The generated product is a small Node.js tenant. The proof pressure is in the
construction graph: two implementation lanes, two test lanes, and one fan-in
must be visible in admitted topology and live runtime frontier evidence.
