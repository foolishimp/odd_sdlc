# T-160 Hello-World Lite Bootstrap

This fixture is a compact conformed authority surface for testing the lite
traversal overlay. The fixture must not include generated product source.

## Product Contract

Build one JavaScript tenant under `build_tenants/hello_world_javascript`.

The generated program must write exactly:

```text
Hello, world!
```

The proof command is:

```bash
node build_tenants/hello_world_javascript/src/hello.js
```

