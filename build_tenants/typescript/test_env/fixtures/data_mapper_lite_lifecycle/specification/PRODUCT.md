# Product

`data_mapper_lite_lifecycle` is a narrow proof product for the data_mapper
domain. It implements the selected LDM topology requirements as a JavaScript
library and test package.

The product must expose graph construction, cardinality validation, and
symbolic dot-path validation behavior. It must include generated tests that
exercise both valid and invalid cases.

