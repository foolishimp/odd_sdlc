# Data Mapper Lite Requirement Subset

This file intentionally contains only the selected data_mapper requirements for
the T-188 tight lifecycle proof.

The full selected requirement text lives in `workspace://specification/REQUIREMENTS.md`.
This helper file exists only to keep the imported-source set explicit without
creating duplicate bare requirement markers.
